name: Morning Audio Brief

on:
  schedule:
    # 6:00am PT (13:00 UTC) — daylight saving time (March–November)
    # Change to '0 14 * * 1-5' during standard time (November–March)
    - cron: '0 13 * * 1-5'
  workflow_dispatch:   # Allow manual trigger from GitHub Actions tab for testing

jobs:
  generate-brief:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install ffmpeg (required by pydub for MP3 handling)
        run: sudo apt-get install -y ffmpeg

      - name: Run pipeline
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS_JSON }}
          GITHUB_PAGES_BASE_URL: ${{ secrets.GITHUB_PAGES_BASE_URL }}
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: python main.py

      - name: Commit and push episode files
        run: |
          git config user.name "Morning Brief Bot"
          git config user.email "action@github.com"
          git add docs/
          git diff --staged --quiet || git commit -m "Daily brief: $(date +%Y-%m-%d)"
          git push
